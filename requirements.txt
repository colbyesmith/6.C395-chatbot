# Minimal deps for HF Space (Gradio + runtime are added by the Space).
# Keep this light so the build succeeds.
huggingface_hub>=0.19.0
python-dotenv>=1.0.0
pandas>=2.0.0
gradio>=4.0.0
# For scripts/ingest_facilities.py Excel support (optional)
openpyxl>=3.0.0
plotly>=5.0.0
folium>=0.15.0
geopy>=2.4.0
requests>=2.28.0
